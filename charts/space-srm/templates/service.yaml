apiVersion: v1
kind: Service
metadata:
  name: {{ include "space-srm.fullname" . }}
  labels:
    {{- include "space-srm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "space-srm.selectorLabels" . | nindent 4 }}
