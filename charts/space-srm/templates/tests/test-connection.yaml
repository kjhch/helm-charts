apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "space-srm.fullname" . }}-test-connection"
  labels:
    {{- include "space-srm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "space-srm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
