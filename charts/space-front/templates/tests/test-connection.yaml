apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "space-front.fullname" . }}-test-connection"
  labels:
    {{- include "space-front.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "space-front.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
