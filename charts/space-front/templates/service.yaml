apiVersion: v1
kind: Service
metadata:
  name: {{ include "space-front.fullname" . }}
  labels:
    {{- include "space-front.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "space-front.selectorLabels" . | nindent 4 }}
